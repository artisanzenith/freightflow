# pnpm configuration for the FreightFlow monorepo.

# Hoist Expo/React Native and their peers so Metro can resolve them.
node-linker=hoisted

# Use a strict but predictable peer-dependency behavior.
strict-peer-dependencies=false

# Keep the lockfile authoritative.
prefer-workspace-packages=true
