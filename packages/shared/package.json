{
  "name": "@freightflow/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript domain types, enums, and constants for FreightFlow.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist .turbo",
    "gen:types": "supabase gen types typescript --linked --schema public > src/supabase.generated.ts"
  },
  "devDependencies": {
    "@freightflow/config": "workspace:*",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2"
  }
}
