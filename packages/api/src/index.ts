/**
 * @freightflow/api
 *
 * Typed Supabase client wrappers and data-access helpers. Apps
 * consume this package instead of hand-writing queries, keeping
 * RLS-aware data access consistent across surfaces.
 *
 * NOTE: Foundation only. Client factory and entity data-access
 * functions are implemented in Phase 1-2 once Supabase is
 * provisioned. No business logic yet.
 */

export const PACKAGE_NAME = '@freightflow/api';
